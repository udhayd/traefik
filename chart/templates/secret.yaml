{{- if .Values.ssl.defaultcrt }}
apiVersion: v1
data:
   tls.crt: {{ .Values.ssl.defaultcrt }}
   tls.key: {{ .Values.ssl.defaultkey }}
kind: Secret
metadata:
  name: public-cert
type: Opaque
{{- end }}
